const express = require('express');
const app = express();
const fs = require("fs");
const port = 3000;

app.use(express.json());




app.post('/subirAlumno', (req, res) => {
let data = req.body.prueba;
console.log(data);

let listadoAlumnos = fs.readFileSync("alumnos.json");
listadoAlumnos = JSON.parse(listadoAlumnos);
res.send(data);
});

app.get('/', (req, res) => {
    res.send('¡Hola, mundo!');
});

app.listen(port, () => {
    console.log(`Servidor escuchando en http://localhost:${port}`);
});